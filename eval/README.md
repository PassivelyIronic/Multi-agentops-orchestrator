# Evaluation

Golden dataset of ~15-20 tasks with expected outcomes (e.g. expected tool
sequence, expected final state), plus an LLM-as-judge harness for scoring
agent runs against it. This is the part most portfolio projects skip —
see the field guide notes on why it matters more than extra features.

Implemented in Phase 5.
