"""
Trace logging: every tool call recorded with input, output, tokens, cost,
and latency. This is the data source the Streamlit dashboard reads from,
and the raw material for the eval harness's cost/latency metrics.

Implemented in Phase 2.
"""
